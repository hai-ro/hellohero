<h2>事業者から条件が提示されている仕事</h2>

<% if hero_signed_in? && @hero.id == current_hero.id %>
  <ul>
    <li><%= link_to "エントリー中の仕事を見る", "/jobs/index/1/hero/#{current_hero.id}" %></li>
    <li><%= link_to "受注している仕事を見る", "/jobs/index/3/hero/#{current_hero.id}" %></li>
    <li><%= link_to "完了した仕事を見る", "/jobs/index/4/hero/#{current_hero.id}" %></li>
  </ul>
<% end %>

<% if @count == 0 %>
  <p>現在、事業者から条件を提示されている仕事はありません。</p>
  <%= link_to "お仕事を探す!", jobs_path %>
<% else %>
  <% @jobs.each do |job| %>
    <p>--------------------------------------------------------------------------------</p>
  
    <p>お仕事名:<%= job.title %></p>
    
    <p>お仕事カテゴリ:<%= job.job_category.name %></p>
    
    <p>発注者:<%= job.nursing_home.id %></p>
    
    <p>報酬額:<%= job.price %>円</p>
    
    <p>開始時間:<%= job.start_datetime %></p>
    
    <p>終了時間:<%= job.end_datetime %></p>
    
    <%= link_to "仕事の詳細を見る", job_path(job.id) %>
  
    <p>--------------------------------------------------------------------------------</p>
  <% end %>
<% end %>
<br />
<br />
<% if hero_signed_in? %>
  <%= link_to "マイページへ戻る", hero_path(current_hero.id) %>
<% elsif nursing_home_signed_in? %>
  <%= link_to "管理ページへ戻る", nursing_home_path(current_nursing_home.id) %>
<% end %>


<p><%= link_to "トップへ戻る", root_path %></p>